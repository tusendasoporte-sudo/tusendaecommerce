---
import { getCurrencies } from '../lib/api';
import { getCurrentStore, getPublicPath } from '../lib/stores';
import { BUSINESS_TYPE_GROUPS } from '../lib/businessTypes';
import { formatBusinessHours, getBusinessHoursStatus } from '../lib/businessHours';

const { settings = {}, showPlatformCta = true } = Astro.props;

const currentStore = await getCurrentStore(Astro);
const currentStoreId = currentStore?.id || settings?.store || '';
const storeName = currentStore?.name || settings?.store_name || settings?.stored_name || 'Tienda';
const homePath = getPublicPath('/', currentStore, Astro.url.pathname);
const privacyPath = getPublicPath('/privacidad', currentStore, Astro.url.pathname);
const logoImageUrl = currentStore?.logoUrl || settings?.logoImageUrl || '';
const whatsappRaw = settings?.whatsapp_number || currentStore?.owner_phone || '';
const whatsappNumber = cleanPhone(whatsappRaw);
const whatsappStoreHref = whatsappNumber
  ? `https://wa.me/${whatsappNumber}?text=${encodeURIComponent(`Hola ${storeName}, quiero información.`)}`
  : '';

const creatorStoreHref = 'https://wa.me/13068006341?text=Hola%2C%20quiero%20crear%20mi%20tienda%20en%20Tu%20Senda%2084.';
const creatorStoreTarget = '_blank';

const socialLinks = normalizeSocialLinks(settings?.footer_social_links);
const socialItems = Object.entries(socialLinks)
  .map(([key, value]) => ({ key, href: normalizeSocialHref(key, value), label: socialLabel(key) }))
  .filter((item) => item.href);
const extraLinks = normalizeExtraLinks(settings);

const trustItems = parseTrustItems(settings?.footer_trust_items);
const province = String(settings?.province || '').trim();
const municipality = String(settings?.municipality || '').trim();
const addressDetail = String(settings?.address_detail || '').trim();
const legacyAddress = String(settings?.address || '').trim();
const locationText = province ? `${province}, ${municipality || 'Cuba'}` : '';
const addressText = addressDetail || (!province && !municipality ? legacyAddress : '');
const storeDescription = String(settings?.store_description || settings?.footer_text || settings?.welcome_text || '').trim();
const aboutStoreText = String(settings?.business_notes || settings?.footer_extra_text || '').trim();
const shippingDeliveryInfo = String(settings?.shipping_delivery_info || '').trim();
const formattedBusinessHours = formatBusinessHours(settings);
const businessHoursStatus = getBusinessHoursStatus(settings);
const businessTypeLabels = new Map(BUSINESS_TYPE_GROUPS.flatMap((group) => group.options.map((option) => [option.slug, option.label])));
const publicBusinessTypes = parseJsonList(settings?.business_types)
  .map((slug) => businessTypeLabels.get(slug) || slug)
  .filter(Boolean)
  .slice(0, 3);

let activeCurrencies: any[] = [];
try {
  activeCurrencies = currentStoreId ? await getCurrencies({ storeId: currentStoreId }) : [];
} catch (_) {
  activeCurrencies = [];
}
const defaultCurrency = Array.isArray(settings?.expand?.default_currency)
  ? settings.expand.default_currency[0]
  : settings?.expand?.default_currency;
const paymentCurrencies = normalizeCurrencies(activeCurrencies, defaultCurrency);

const locationDescription = [locationText, addressText].filter(Boolean).join(' - ');
const contextualUtility = formattedBusinessHours
  ? {
      id: 'hours',
      label: 'Horarios',
      detail: businessHoursStatus.mode === 'temporarily_closed'
        ? 'Cerrado temporalmente'
        : businessHoursStatus.isAvailable
          ? 'Disponible ahora'
          : 'Ver horario semanal',
      icon: 'clock',
      kind: 'modal',
    }
  : shippingDeliveryInfo
    ? { id: 'shipping', label: 'Envíos', detail: 'Opciones de entrega', icon: 'truck', kind: 'modal' }
    : locationDescription
      ? { id: 'location', label: 'Ubicación', detail: 'Ver dirección', icon: 'location', kind: 'modal' }
      : aboutStoreText
        ? { id: 'about', label: 'Información', detail: 'Sobre la tienda', icon: 'info', kind: 'modal' }
        : { id: 'catalog', label: 'Catálogo', detail: 'Ver productos', icon: 'bag', kind: 'link', href: homePath, external: false };
const utilityItems = [
  paymentCurrencies.length
    ? { id: 'payments', label: 'Formas de pago', detail: 'Métodos disponibles', icon: 'payment', kind: 'modal' }
    : null,
  contextualUtility,
  whatsappStoreHref
    ? { id: 'whatsapp', label: 'WhatsApp', detail: 'Atención directa', icon: 'whatsapp', kind: 'link', href: whatsappStoreHref, external: true }
    : null,
].filter(Boolean);
const footerTagline = storeDescription || 'Todo lo que necesitas, en un solo lugar.';

function cleanPhone(value: any) {
  return String(value || '').replace(/[^0-9]/g, '');
}

function storeInitials(name: string) {
  return String(name || 'Tienda')
    .split(/\s+/)
    .filter(Boolean)
    .slice(0, 2)
    .map((part) => part[0]?.toUpperCase())
    .join('') || 'T';
}

function parseJsonObject(value: any) {
  if (value && typeof value === 'object' && !Array.isArray(value)) return value;
  if (typeof value !== 'string' || !value.trim()) return {};
  try {
    const parsed = JSON.parse(value);
    return parsed && typeof parsed === 'object' && !Array.isArray(parsed) ? parsed : {};
  } catch (_) {
    return {};
  }
}

function parseJsonList(value: any) {
  if (Array.isArray(value)) return value.filter(Boolean).map(String);
  if (typeof value !== 'string' || !value.trim()) return [];
  try {
    const parsed = JSON.parse(value);
    return Array.isArray(parsed) ? parsed.filter(Boolean).map(String) : [];
  } catch (_) {
    return value.split(',').map((item) => item.trim()).filter(Boolean);
  }
}

function normalizeSocialLinks(value: any) {
  const parsed = parseJsonObject(value);
  return {
    instagram: String(parsed.instagram || '').trim(),
    facebook: String(parsed.facebook || '').trim(),
    tiktok: String(parsed.tiktok || '').trim(),
    youtube: String(parsed.youtube || '').trim(),
    whatsapp: String(parsed.whatsapp || '').trim(),
  };
}

function normalizeSocialHref(key: string, value: string) {
  const raw = String(value || '').trim();
  if (!raw) return '';
  if (key === 'whatsapp') {
    if (/^https?:\/\//i.test(raw)) return raw;
    const phone = cleanPhone(raw);
    return phone ? `https://wa.me/${phone}` : '';
  }
  if (!/^https?:\/\//i.test(raw)) return '';
  return raw;
}

function normalizeExtraLinks(source: any) {
  return [
    {
      active: source?.footer_link_1_active !== false,
      label: String(source?.footer_link_1_label || '').trim(),
      href: normalizePlainHref(source?.footer_link_1_url),
    },
    {
      active: source?.footer_link_2_active !== false,
      label: String(source?.footer_link_2_label || '').trim(),
      href: normalizePlainHref(source?.footer_link_2_url),
    },
  ].filter((item) => item.active && item.label && item.href);
}

function normalizePlainHref(value: any) {
  const raw = String(value || '').trim();
  return /^https?:\/\/\S+\.\S+/i.test(raw) ? raw : '';
}

function socialLabel(key: string) {
  return ({
    instagram: 'Instagram',
    facebook: 'Facebook',
    tiktok: 'TikTok',
    youtube: 'YouTube',
    whatsapp: 'WhatsApp',
  } as Record<string, string>)[key] || key;
}

function parseTrustItems(value: any) {
  let rawItems: any[] = [];
  if (Array.isArray(value)) {
    rawItems = value;
  } else if (typeof value === 'string' && value.trim()) {
    try {
      const parsed = JSON.parse(value);
      rawItems = Array.isArray(parsed) ? parsed : [];
    } catch (_) {
      rawItems = [];
    }
  }
  return rawItems
    .filter((item) => item && typeof item === 'object')
    .map((item) => ({
      icon: allowedIcon(item.icon),
      title: String(item.title || '').trim(),
      description: String(item.description || '').trim(),
      active: item.active !== false,
    }))
    .filter((item) => item.active && item.title)
    .slice(0, 4);
}

function allowedIcon(icon: any) {
  const value = String(icon || '').trim();
  return ['shield', 'lock', 'truck', 'headset', 'star', 'gift', 'location', 'bolt', 'heart', 'bag', 'check', 'whatsapp'].includes(value) ? value : 'check';
}

function normalizeCurrencies(currencies: any[], fallbackCurrency: any) {
  const list = (currencies || [])
    .filter((currency) => currency && currency.active !== false)
    .map(currencyView);
  if (list.length) return list;
  return fallbackCurrency ? [currencyView(fallbackCurrency)] : [];
}

function currencyView(currency: any) {
  const code = String(currency?.code || '').trim().toUpperCase();
  const name = String(currency?.name || code).trim();
  const symbol = String(currency?.symbol || '').trim();
  return {
    code,
    label: [code, name && name !== code ? name : '', symbol && symbol !== code ? `(${symbol})` : ''].filter(Boolean).join(' '),
  };
}

function iconSvg(name: string) {
  const premiumUtilityImages: Record<string, string> = {
    payment: '/brand/footer-icon-payment-v2.webp',
    bag: '/brand/footer-icon-catalog-v2.webp',
    whatsapp: '/brand/footer-icon-whatsapp-v2.webp',
  };
  if (premiumUtilityImages[name]) {
    return `<img src="${premiumUtilityImages[name]}" alt="" aria-hidden="true" width="256" height="256" loading="lazy" decoding="async" />`;
  }
  const premiumUtilityIcons: Record<string, string> = {
    payment: '<svg viewBox="0 0 64 64" aria-hidden="true"><defs><linearGradient id="pz-payment-gradient" x1="15" y1="12" x2="50" y2="51" gradientUnits="userSpaceOnUse"><stop stop-color="#75c9ff"/><stop offset=".28" stop-color="#429eff"/><stop offset=".66" stop-color="#1768e7"/><stop offset="1" stop-color="#0b3cae"/></linearGradient><linearGradient id="pz-payment-edge" x1="32" y1="14" x2="32" y2="51" gradientUnits="userSpaceOnUse"><stop stop-color="#bce9ff"/><stop offset=".16" stop-color="#4aa7ff"/><stop offset=".78" stop-color="#0a45be"/><stop offset="1" stop-color="#062b84"/></linearGradient><filter id="pz-payment-depth" x="-35%" y="-35%" width="170%" height="190%"><feDropShadow dx="0" dy="5" stdDeviation="4" flood-color="#020817" flood-opacity=".86"/><feDropShadow dx="0" dy="0" stdDeviation="2.4" flood-color="#268cff" flood-opacity=".72"/></filter></defs><g filter="url(#pz-payment-depth)"><rect x="9" y="13" width="46" height="37" rx="8" fill="#061b55" stroke="#1765dc" stroke-width="2.4"/><rect x="11.5" y="11" width="43" height="35" rx="7" fill="url(#pz-payment-gradient)" stroke="url(#pz-payment-edge)" stroke-width="2"/><path d="M11.5 20.5h43v10.2h-43z" fill="#082b78"/><path d="M12.5 21.5h41" stroke="#0b1d50" stroke-width="3" opacity=".82"/><path d="M16 14.7h28" stroke="#d6f2ff" stroke-width="1.5" stroke-linecap="round" opacity=".72"/><rect x="16" y="35.5" width="14.5" height="5.2" rx="2.1" fill="#061944" stroke="#5eb4ff" stroke-width="1.1"/><circle cx="47" cy="38.2" r="2.5" fill="#65beff" opacity=".82"/><path d="M14 43.5h36" stroke="#083386" stroke-width="1.5" opacity=".7"/></g></svg>',
    bag: '<svg viewBox="0 0 64 64" aria-hidden="true"><defs><linearGradient id="pz-catalog-gradient" x1="18" y1="14" x2="47" y2="53" gradientUnits="userSpaceOnUse"><stop stop-color="#8bd5ff"/><stop offset=".26" stop-color="#4da8ff"/><stop offset=".68" stop-color="#1c6de5"/><stop offset="1" stop-color="#0b43b5"/></linearGradient><linearGradient id="pz-catalog-side" x1="30" y1="26" x2="45" y2="51" gradientUnits="userSpaceOnUse"><stop stop-color="#3e95f4"/><stop offset="1" stop-color="#0a358e"/></linearGradient><filter id="pz-catalog-depth" x="-40%" y="-40%" width="180%" height="200%"><feDropShadow dx="0" dy="5" stdDeviation="4" flood-color="#020817" flood-opacity=".86"/><feDropShadow dx="0" dy="0" stdDeviation="2.2" flood-color="#2e8cff" flood-opacity=".66"/></filter></defs><g filter="url(#pz-catalog-depth)"><path d="M14.5 24.5h35l-2.8 28H17.3l-2.8-28Z" fill="#061b55" stroke="#1765dc" stroke-width="2.2"/><path d="M16.5 22h31.2l-2.5 27.5H19L16.5 22Z" fill="url(#pz-catalog-gradient)" stroke="#70bdff" stroke-width="1.7"/><path d="M36.5 22h11.2l-2.5 27.5H34.8L36.5 22Z" fill="url(#pz-catalog-side)" opacity=".68"/><path d="M23 23v-4.8a9 9 0 0 1 18 0V23" fill="none" stroke="#9edcff" stroke-width="4.2" stroke-linecap="round"/><path d="M25.5 22v-3.4a6.5 6.5 0 0 1 13 0V22" fill="none" stroke="#0b3c9b" stroke-width="1.7" stroke-linecap="round"/><path d="M21 26.5h20" stroke="#d9f3ff" stroke-width="1.6" stroke-linecap="round" opacity=".68"/><path d="M24 34c2 2.4 4.7 3.6 8 3.6s6-1.2 8-3.6" fill="none" stroke="#07245e" stroke-width="3" stroke-linecap="round"/></g></svg>',
    whatsapp: '<svg viewBox="0 0 64 64" aria-hidden="true"><defs><linearGradient id="pz-whatsapp-gradient" x1="18" y1="10" x2="47" y2="54" gradientUnits="userSpaceOnUse"><stop stop-color="#8affca"/><stop offset=".28" stop-color="#48f59a"/><stop offset=".7" stop-color="#17cb6c"/><stop offset="1" stop-color="#07964c"/></linearGradient><linearGradient id="pz-whatsapp-highlight" x1="20" y1="13" x2="38" y2="38" gradientUnits="userSpaceOnUse"><stop stop-color="#e1fff1"/><stop offset="1" stop-color="#6fffb4" stop-opacity="0"/></linearGradient><filter id="pz-whatsapp-depth" x="-40%" y="-40%" width="180%" height="200%"><feDropShadow dx="0" dy="5" stdDeviation="4" flood-color="#020817" flood-opacity=".9"/><feDropShadow dx="0" dy="0" stdDeviation="3" flood-color="#28f58a" flood-opacity=".72"/></filter></defs><g filter="url(#pz-whatsapp-depth)"><path d="M32 7.5c-13 0-23.5 10.2-23.5 22.8 0 4.3 1.2 8.4 3.4 11.9L8.2 56.5l14.9-3.8A24 24 0 0 0 32 54.4c13 0 23.5-10.2 23.5-22.8S45 7.5 32 7.5Z" fill="#041d24" stroke="#0c8a50" stroke-width="2.4"/><path d="M32 9.8c-11.7 0-21.2 9.2-21.2 20.5 0 4 1.2 7.7 3.3 10.8l-3 11.5 12-3a21.7 21.7 0 0 0 8.9 1.9c11.7 0 21.2-9.2 21.2-20.6C53.2 19.3 43.7 9.8 32 9.8Zm0 5.3c8.6 0 15.7 6.9 15.7 15.5S40.6 46 32 46c-3 0-5.8-.8-8.2-2.2l-6.2 1.6 1.6-6a15.2 15.2 0 0 1-2.8-8.8c0-8.6 7-15.5 15.6-15.5Z" fill="url(#pz-whatsapp-gradient)"/><path d="M31.8 12.2c-7.7 0-14.2 5.6-15.3 12.9 3.3-5.5 9.4-9.2 16.4-9.2 4.8 0 9.1 1.7 12.4 4.6a15.8 15.8 0 0 0-13.5-8.3Z" fill="url(#pz-whatsapp-highlight)" opacity=".72"/><path d="M24.4 21.5c-.8 0-1.5.3-2.1 1.1-.8 1-2 2.6-2 5.1 0 2.6 2 5.1 2.3 5.5.3.4 4 6.3 10 8.7 5.1 2 6.1 1.6 7.2 1.5 1.1-.1 3.4-1.3 3.9-2.7.5-1.3.5-2.5.3-2.7-.2-.3-.8-.5-1.7-.9l-3.8-1.8c-.6-.3-1.2-.2-1.7.5l-1.5 1.9c-.5.5-1 .5-1.8.2-1-.4-3.2-1.2-5.8-3.5-2.2-1.9-3.6-4.2-4-5-.5-.7 0-1.1.3-1.5l1.1-1.3c.3-.4.5-.8.7-1.2.2-.5.1-.9-.1-1.4l-1.6-3.8c-.3-.6-.8-.8-1.5-.8h-2.2Z" fill="#eafff4" stroke="#b6ffda" stroke-width=".5"/></g></svg>',
  };
  if (premiumUtilityIcons[name]) return premiumUtilityIcons[name];
  const paths: Record<string, string> = {
    payment: '<path d="M3 5h18a2 2 0 0 1 2 2v10a2 2 0 0 1-2 2H3a2 2 0 0 1-2-2V7a2 2 0 0 1 2-2Zm0 4h18V7H3v2Zm2 5v2h6v-2H5Z"/>',
    clock: '<path d="M12 2a10 10 0 1 0 0 20 10 10 0 0 0 0-20Zm0 2a8 8 0 1 1 0 16 8 8 0 0 1 0-16Zm1 3h-2v6l4.6 2.7 1-1.7-3.6-2.1V7Z"/>',
    info: '<path d="M11 10h2v8h-2v-8Zm1-6a1.5 1.5 0 1 1 0 3 1.5 1.5 0 0 1 0-3Zm0-2a10 10 0 1 0 0 20 10 10 0 0 0 0-20Zm0 2a8 8 0 1 1 0 16 8 8 0 0 1 0-16Z"/>',
    shield: '<path d="M12 3 5 6v5c0 4.4 2.8 8.4 7 10 4.2-1.6 7-5.6 7-10V6l-7-3Zm0 4.4 4 1.7v2c0 2.9-1.5 5.6-4 7-2.5-1.4-4-4.1-4-7v-2l4-1.7Zm3.5 3.1-4.2 4.2-2-2 1.4-1.4.6.6 2.8-2.8 1.4 1.4Z"/>',
    lock: '<path d="M17 9V7a5 5 0 0 0-10 0v2H5v12h14V9h-2Zm-8 0V7a3 3 0 0 1 6 0v2H9Zm4 7.7V18h-2v-1.3a2 2 0 1 1 2 0Z"/>',
    truck: '<path d="M3 6h11v8h2.2l1.8 2H21v-5.2L18.2 8H16V6h3l4 5v7h-2.2a3 3 0 0 1-5.6 0H9.8a3 3 0 0 1-5.6 0H2v-4h2V8H3V6Zm3 12a1 1 0 1 0 0-2 1 1 0 0 0 0 2Zm12 0a1 1 0 1 0 0-2 1 1 0 0 0 0 2Z"/>',
    headset: '<path d="M12 3a8 8 0 0 0-8 8v4a3 3 0 0 0 3 3h2v-7H6a6 6 0 0 1 12 0h-3v7h2.2a5.2 5.2 0 0 1-4.2 2H11v2h2a7 7 0 0 0 7-7v-4a8 8 0 0 0-8-8Z"/>',
    star: '<path d="m12 2 2.8 6 6.5.8-4.8 4.5 1.2 6.4L12 16.5l-5.7 3.2 1.2-6.4-4.8-4.5 6.5-.8L12 2Z"/>',
    gift: '<path d="M20 7h-2.2A3 3 0 0 0 12 5.8 3 3 0 0 0 6.2 7H4v5h1v9h14v-9h1V7ZM9 5a1 1 0 0 1 1 1v1H9a1 1 0 0 1 0-2Zm6 0a1 1 0 0 1 0 2h-1V6a1 1 0 0 1 1-1Zm-8 7h4v7H7v-7Zm6 7v-7h4v7h-4Z"/>',
    location: '<path d="M12 2a7 7 0 0 0-7 7c0 5.2 7 13 7 13s7-7.8 7-13a7 7 0 0 0-7-7Zm0 9.5A2.5 2.5 0 1 1 12 6a2.5 2.5 0 0 1 0 5.5Z"/>',
    bolt: '<path d="M13 2 4 14h6l-1 8 10-13h-6l0-7Z"/>',
    heart: '<path d="M12 21s-8-4.7-8-11a4.8 4.8 0 0 1 8-3.6A4.8 4.8 0 0 1 20 10c0 6.3-8 11-8 11Z"/>',
    bag: '<path d="M7 8V7a5 5 0 0 1 10 0v1h3v13H4V8h3Zm2 0h6V7a3 3 0 0 0-6 0v1Z"/>',
    check: '<path d="M9.2 16.2 4.8 11.8 3.4 13.2 9.2 19 21 7.2 19.6 5.8 9.2 16.2Z"/>',
    whatsapp: '<path d="M12 2a9 9 0 0 0-7.7 13.7L3 22l6.4-1.2A9 9 0 1 0 12 2Zm0 2a7 7 0 1 1-2.1 13.7l-.4-.1-3.9.7.8-3.8-.2-.4A7 7 0 0 1 12 4Zm-3 3.8c-.2 0-.6.1-.9.5-.3.4-.8 1-.8 2.1s.8 2.2 1 2.4c.1.2 1.7 2.7 4.2 3.7 2.1.9 2.5.7 3 .7.4 0 1.4-.6 1.6-1.1.2-.6.2-1 .1-1.1-.1-.1-.3-.2-.7-.4l-1.6-.8c-.2-.1-.5-.1-.7.2l-.6.8c-.2.2-.4.2-.7.1-.4-.2-1.3-.5-2.4-1.5-.9-.8-1.5-1.8-1.7-2.1-.2-.3 0-.5.1-.6l.5-.6c.1-.2.2-.3.3-.5.1-.2.1-.4 0-.6L9 8.2c-.1-.3-.3-.4-.6-.4H9Z"/>',
    instagram: '<path d="M7 2h10a5 5 0 0 1 5 5v10a5 5 0 0 1-5 5H7a5 5 0 0 1-5-5V7a5 5 0 0 1 5-5Zm0 2a3 3 0 0 0-3 3v10a3 3 0 0 0 3 3h10a3 3 0 0 0 3-3V7a3 3 0 0 0-3-3H7Zm5 4a4 4 0 1 1 0 8 4 4 0 0 1 0-8Zm0 2a2 2 0 1 0 0 4 2 2 0 0 0 0-4Zm5.2-3.2a1 1 0 1 1 0 2 1 1 0 0 1 0-2Z"/>',
    facebook: '<path d="M14 8h3V4h-3a5 5 0 0 0-5 5v3H6v4h3v6h4v-6h3l1-4h-4V9a1 1 0 0 1 1-1Z"/>',
    tiktok: '<path d="M15 3c.4 2.5 1.8 4 4 4.3V11a7 7 0 0 1-4-1.3V16a6 6 0 1 1-6-6c.4 0 .7 0 1 .1v4.1A2 2 0 1 0 11 16V3h4Z"/>',
    youtube: '<path d="M21.6 7.2a3 3 0 0 0-2.1-2.1C17.6 4.6 12 4.6 12 4.6s-5.6 0-7.5.5a3 3 0 0 0-2.1 2.1A31 31 0 0 0 2 12a31 31 0 0 0 .4 4.8 3 3 0 0 0 2.1 2.1c1.9.5 7.5.5 7.5.5s5.6 0 7.5-.5a3 3 0 0 0 2.1-2.1A31 31 0 0 0 22 12a31 31 0 0 0-.4-4.8ZM10 15.2V8.8l5.5 3.2-5.5 3.2Z"/>',
  };
  return `<svg viewBox="0 0 24 24" aria-hidden="true" fill="currentColor">${paths[name] || paths.check}</svg>`;
}
---

{trustItems.length > 0 && (
  <section class="pz-trust-strip" aria-label="Beneficios de la tienda">
    <div class={`pz-trust-grid count-${trustItems.length}`}>
      {trustItems.map((item) => (
        <article class="pz-trust-item">
          <span class="pz-trust-icon" set:html={iconSvg(item.icon)} />
          <span class="pz-trust-copy">
            <strong>{item.title}</strong>
            {item.description && <small>{item.description}</small>}
          </span>
        </article>
      ))}
    </div>
  </section>
)}

<footer class="pz-public-footer" aria-label="Información de la tienda">
  <div class="pz-footer-main">
    <section class="pz-footer-store" aria-label={storeName}>
      <a class="pz-footer-logo-link" href={homePath} aria-label={`Ir al inicio de ${storeName}`}>
        {logoImageUrl ? <img src={logoImageUrl} alt={storeName} /> : <span>{storeInitials(storeName)}</span>}
      </a>
      <div class="pz-footer-brand-copy">
        <h2>{storeName}</h2>
        {publicBusinessTypes.length > 0 && <p class="pz-footer-types">{publicBusinessTypes.join(' • ')}</p>}
      </div>
      <p class="pz-footer-description">{footerTagline}</p>
      {socialItems.length > 0 && (
        <div class="pz-footer-social" aria-label="Redes sociales">
          {socialItems.map((item) => (
            <a href={item.href} target="_blank" rel="noreferrer" aria-label={item.label} title={item.label} set:html={iconSvg(item.key)} />
          ))}
        </div>
      )}
    </section>

    {utilityItems.length > 0 && (
      <section class={`pz-footer-utilities count-${utilityItems.length}`} aria-label="Información útil">
        {utilityItems.map((item) => (
          item.kind === 'link'
            ? (
              <a class={`pz-footer-utility is-${item.id}`} href={item.href} target={item.external ? '_blank' : undefined} rel={item.external ? 'noreferrer' : undefined}>
                <span class="pz-footer-utility-icon" set:html={iconSvg(item.icon)} />
                <span class="pz-footer-utility-copy"><strong>{item.label}</strong></span>
              </a>
            )
            : (
              <button class={`pz-footer-utility is-${item.id}`} type="button" data-pz-footer-modal-open={item.id}>
                <span class="pz-footer-utility-icon" set:html={iconSvg(item.icon)} />
                <span class="pz-footer-utility-copy"><strong>{item.label}</strong></span>
              </button>
            )
        ))}
      </section>
    )}

    <nav class="pz-footer-secondary-links" aria-label="Más información">
        {aboutStoreText && contextualUtility?.id !== 'about' && <button type="button" data-pz-footer-modal-open="about">Sobre la tienda</button>}
        <a href={privacyPath}>Privacidad y eliminación de datos</a>
        {extraLinks.map((item) => (
          <a href={item.href} target="_blank" rel="noreferrer">{item.label}</a>
        ))}
    </nav>

    {showPlatformCta && <section class="pz-footer-platform-cta" aria-label="Crear tienda en Tu Senda 84">
      <div class="pz-platform-copy">
        <span class="pz-platform-eyebrow">Para emprendedores</span>
        <h3>Convierte tu negocio en una tienda online</h3>
        <p>Conoce la plataforma de Tu Senda 84 y crea tu propio espacio para vender.</p>
      </div>
      <div class="pz-platform-visual" aria-hidden="true">
        <img src="/brand/tusenda84-storefront-platform-v2.webp" alt="" loading="lazy" decoding="async" width="840" height="1050" />
      </div>
      <a class="pz-platform-button" href={creatorStoreHref} target={creatorStoreTarget} rel="noopener noreferrer">Quiero crear mi tienda</a>
      <button class="pz-platform-learn" type="button" data-pz-footer-modal-open="creator-info">
        Ver cómo funciona <span aria-hidden="true">›</span>
      </button>
    </section>}

    {showPlatformCta && <div class="pz-footer-platform-backed" aria-label="Tecnología de Tu Senda 84">
        <span class="pz-tusenda-mark" aria-hidden="true">
          <img src="/brand/tusenda84-bazzar-logo.webp" alt="" loading="lazy" />
        </span>
        <span><small>Tecnología de</small><strong>Tu Senda 84</strong></span>
    </div>}

  </div>
</footer>

{aboutStoreText && (
  <div class="pz-footer-modal-overlay" data-pz-footer-modal="about" hidden>
    <section class="pz-footer-modal" role="dialog" aria-modal="true" aria-labelledby="pz-footer-about-title">
      <button class="pz-footer-modal-close" type="button" data-pz-footer-modal-close aria-label="Cerrar">×</button>
      <h2 id="pz-footer-about-title">Sobre la tienda</h2>
      <p>{aboutStoreText}</p>
    </section>
  </div>
)}

{shippingDeliveryInfo && (
  <div class="pz-footer-modal-overlay" data-pz-footer-modal="shipping" hidden>
    <section class="pz-footer-modal" role="dialog" aria-modal="true" aria-labelledby="pz-footer-shipping-title">
      <button class="pz-footer-modal-close" type="button" data-pz-footer-modal-close aria-label="Cerrar">×</button>
      <h2 id="pz-footer-shipping-title">Envíos y entregas</h2>
      <p>{shippingDeliveryInfo}</p>
    </section>
  </div>
)}

{formattedBusinessHours && (
  <div class="pz-footer-modal-overlay" data-pz-footer-modal="hours" hidden>
    <section class="pz-footer-modal" role="dialog" aria-modal="true" aria-labelledby="pz-footer-hours-title">
      <button class="pz-footer-modal-close" type="button" data-pz-footer-modal-close aria-label="Cerrar">×</button>
      <h2 id="pz-footer-hours-title">Horarios</h2>
      {businessHoursStatus.title && <p><strong>{businessHoursStatus.title}</strong></p>}
      <p>{formattedBusinessHours}</p>
    </section>
  </div>
)}

{locationDescription && (
  <div class="pz-footer-modal-overlay" data-pz-footer-modal="location" hidden>
    <section class="pz-footer-modal" role="dialog" aria-modal="true" aria-labelledby="pz-footer-location-title">
      <button class="pz-footer-modal-close" type="button" data-pz-footer-modal-close aria-label="Cerrar">×</button>
      <h2 id="pz-footer-location-title">Ubicación</h2>
      <p>{locationDescription}</p>
    </section>
  </div>
)}

{paymentCurrencies.length > 0 && (
  <div class="pz-footer-modal-overlay" data-pz-footer-modal="payments" hidden>
    <section class="pz-footer-modal" role="dialog" aria-modal="true" aria-labelledby="pz-footer-payments-title">
      <button class="pz-footer-modal-close" type="button" data-pz-footer-modal-close aria-label="Cerrar">×</button>
      <h2 id="pz-footer-payments-title">Formas de pago</h2>
      <p>Monedas aceptadas por esta tienda</p>
      <ul>
        {paymentCurrencies.map((currency) => <li>{currency.label}</li>)}
      </ul>
      <p>El pago se coordina directamente con la tienda al confirmar el pedido.</p>
    </section>
  </div>
)}

{showPlatformCta && (
  <div class="pz-footer-modal-overlay" data-pz-footer-modal="creator-info" hidden>
    <section class="pz-footer-modal" role="dialog" aria-modal="true" aria-labelledby="pz-footer-creator-title">
      <button class="pz-footer-modal-close" type="button" data-pz-footer-modal-close aria-label="Cerrar">×</button>
      <h2 id="pz-footer-creator-title">Cómo crear tu tienda</h2>
      <p>Cuéntanos sobre tu negocio y el equipo de Tu Senda 84 te explicará el proceso, las herramientas disponibles y los próximos pasos.</p>
      <a class="pz-footer-modal-action" href={creatorStoreHref} target={creatorStoreTarget} rel="noopener noreferrer">Hablar por WhatsApp</a>
    </section>
  </div>
)}

<script>
  (() => {
    const overlays = Array.from(document.querySelectorAll('[data-pz-footer-modal]'));
    function openFooterModal(id) {
      const overlay = overlays.find((item) => item.dataset.pzFooterModal === id);
      if (!overlay) return;
      overlay.hidden = false;
      requestAnimationFrame(() => overlay.classList.add('open'));
    }
    function closeFooterModal(overlay) {
      if (!overlay) return;
      overlay.classList.remove('open');
      setTimeout(() => { if (!overlay.classList.contains('open')) overlay.hidden = true; }, 160);
    }
    document.querySelectorAll('[data-pz-footer-modal-open]').forEach((button) => {
      button.addEventListener('click', () => openFooterModal(button.dataset.pzFooterModalOpen));
    });
    overlays.forEach((overlay) => {
      overlay.addEventListener('click', (event) => {
        if (event.target === overlay || event.target.closest('[data-pz-footer-modal-close]')) closeFooterModal(overlay);
      });
    });
    document.addEventListener('keydown', (event) => {
      if (event.key === 'Escape') overlays.forEach(closeFooterModal);
    });
  })();
</script>

<style>
  .pz-trust-strip { margin: 28px 0 18px; border-radius: 8px; background: rgba(255,255,255,.96); border: 1px solid #e5edf7; box-shadow: 0 18px 45px rgba(15,23,42,.08); overflow: hidden; }
  .pz-trust-grid { display: grid; grid-template-columns: repeat(4, minmax(0, 1fr)); }
  .pz-trust-grid.count-1 { grid-template-columns: 1fr; }
  .pz-trust-grid.count-2 { grid-template-columns: repeat(2, minmax(0, 1fr)); }
  .pz-trust-grid.count-3 { grid-template-columns: repeat(3, minmax(0, 1fr)); }
  .pz-trust-item { min-width: 0; display: flex; align-items: center; gap: 16px; padding: 24px; border-right: 1px solid #e5edf7; }
  .pz-trust-item:last-child { border-right: 0; }
  .pz-trust-icon { width: 58px; height: 58px; border-radius: 8px; display: inline-flex; align-items: center; justify-content: center; background: #eff6ff; color: #0b63f6; flex: 0 0 auto; }
  .pz-trust-icon svg { width: 34px; height: 34px; fill: currentColor; }
  .pz-trust-copy { min-width: 0; display: grid; gap: 5px; color: #0b1744; }
  .pz-trust-copy strong { font-size: 17px; line-height: 1.15; font-weight: 1000; }
  .pz-trust-copy small { color: #33446f; font-size: 14px; line-height: 1.35; font-weight: 800; }
  .pz-public-footer { width: 100%; max-width: 100%; min-width: 0; margin-top: 22px; border-radius: 20px; border: 1px solid rgba(147,197,253,.18); border-top-color: rgba(191,219,254,.28); background: radial-gradient(circle at 14% 0%, rgba(59,130,246,.30), transparent 34%), radial-gradient(circle at 92% 18%, rgba(14,165,233,.16), transparent 28%), linear-gradient(135deg, #061b45 0%, #082f6f 45%, #03132f 100%); color: #fff; overflow: hidden; box-shadow: 0 28px 78px rgba(3,19,47,.28), inset 0 1px 0 rgba(255,255,255,.08); }
  .pz-footer-main { width: 100%; max-width: 100%; min-width: 0; padding: clamp(30px, 5vw, 54px); display: grid; grid-template-columns: minmax(0, 1.18fr) minmax(190px, .78fr) minmax(230px, .92fr); gap: clamp(28px, 4.8vw, 72px); align-items: start; }
  .pz-footer-store { min-width: 0; display: grid; grid-template-columns: 76px minmax(0, 1fr); gap: 16px; align-items: center; }
  .pz-footer-logo-link { width: 68px; height: 68px; border-radius: 15px; background: linear-gradient(135deg, #1d7cff, #0753d8); color: #fff; display: inline-flex; align-items: center; justify-content: center; overflow: hidden; flex: 0 0 auto; text-decoration: none; font-weight: 1000; font-size: 25px; border: 1px solid rgba(255,255,255,.22); box-shadow: 0 18px 38px rgba(0,0,0,.26), inset 0 1px 0 rgba(255,255,255,.26); }
  .pz-footer-logo-link img { width: 100%; height: 100%; object-fit: contain; }
  .pz-footer-logo-link span { display: inline-flex; align-items: center; justify-content: center; width: 100%; height: 100%; }
  .pz-footer-brand-copy { min-width: 0; }
  .pz-footer-brand-copy h2 { max-width: 100%; margin: 0; color: #fff; font-size: clamp(26px, 3vw, 36px); line-height: 1.08; font-weight: 1000; letter-spacing: 0; overflow-wrap: anywhere; word-break: break-word; text-shadow: 0 10px 28px rgba(0,0,0,.18); }
  .pz-footer-types { margin: 7px 0 0; color: rgba(219,234,254,.82); font-size: 14px; line-height: 1.35; font-weight: 850; }
  .pz-footer-description { grid-column: 1 / -1; max-width: 520px; margin: 20px 0 0; color: rgba(239,246,255,.88); font-size: 16px; line-height: 1.65; font-weight: 750; white-space: pre-line; }
  .pz-footer-social { grid-column: 1 / -1; display: flex; flex-wrap: wrap; gap: 12px; margin-top: 20px; }
  .pz-footer-social a { width: 48px; height: 48px; border-radius: 999px; display: inline-flex; align-items: center; justify-content: center; background: rgba(255,255,255,.105); color: #fff; border: 1px solid rgba(191,219,254,.18); text-decoration: none; box-shadow: inset 0 1px 0 rgba(255,255,255,.10); transition: transform .18s ease, background .18s ease, border-color .18s ease, color .18s ease; }
  .pz-footer-social a:hover { transform: translateY(-2px); background: rgba(219,234,254,.18); border-color: rgba(147,197,253,.42); color: #bfdbfe; }
  .pz-footer-social svg, .pz-footer-whatsapp-btn svg { width: 24px; height: 24px; fill: currentColor; flex: 0 0 auto; }
  .pz-footer-info, .pz-footer-contact { min-width: 0; max-width: 100%; }
  .pz-footer-info h3, .pz-footer-contact h3 { margin: 0 0 22px; font-size: 22px; line-height: 1.12; font-weight: 1000; color: #fff; overflow-wrap: anywhere; }
  .pz-footer-info nav { display: grid; gap: 17px; }
  .pz-footer-info a, .pz-footer-info button { width: max-content; max-width: 100%; min-width: 0; padding: 0; border: 0; background: transparent; color: rgba(239,246,255,.84); font: inherit; font-size: 16px; line-height: 1.45; font-weight: 850; overflow-wrap: anywhere; white-space: normal; text-align: left; text-decoration: none; cursor: pointer; transition: color .18s ease, transform .18s ease; }
  .pz-footer-info a:hover, .pz-footer-info button:hover { color: #93c5fd; transform: translateX(2px); }
  .pz-footer-contact { min-width: 0; display: grid; gap: 15px; }
  .pz-footer-contact-line { margin: 0; min-width: 0; display: grid; grid-template-columns: 46px minmax(0, 1fr); align-items: center; gap: 12px; color: rgba(239,246,255,.88); font-size: 16px; line-height: 1.45; font-weight: 850; text-decoration: none; }
  .pz-footer-contact-icon { width: 46px; height: 46px; border-radius: 16px; display: grid; place-items: center; flex: 0 0 auto; color: #fff; background: linear-gradient(145deg, rgba(255,255,255,.16), rgba(59,130,246,.16)); border: 1px solid rgba(147,197,253,.26); box-shadow: 0 12px 24px rgba(0,0,0,.16), inset 0 1px 0 rgba(255,255,255,.12); }
  .pz-footer-contact-icon svg { width: 22px; height: 22px; display: block; color: currentColor; fill: currentColor !important; }
  .pz-footer-contact-icon svg path { fill: currentColor !important; }
  .pz-footer-contact-copy { min-width: 0; overflow-wrap: anywhere; color: rgba(255,255,255,.90); font-weight: 850; line-height: 1.35; }
  .pz-contact-location .pz-footer-contact-icon { color: #fff; background: linear-gradient(145deg, rgba(59,130,246,.38), rgba(37,99,235,.18)); border-color: rgba(147,197,253,.42); }
  .pz-footer-whatsapp-btn { width: min(100%, 250px); min-height: 58px; margin-top: 10px; display: inline-flex; align-items: center; justify-content: center; gap: 10px; border-radius: 13px; padding: 14px 20px; background: linear-gradient(135deg, #1272ff, #0757e8); color: #fff; text-decoration: none; font-size: 16px; font-weight: 1000; border: 1px solid rgba(255,255,255,.16); box-shadow: 0 18px 38px rgba(11,99,246,.30), inset 0 1px 0 rgba(255,255,255,.22); transition: transform .18s ease, box-shadow .18s ease, filter .18s ease; }
  .pz-footer-whatsapp-btn:hover { transform: translateY(-2px); filter: brightness(1.04); box-shadow: 0 22px 46px rgba(11,99,246,.36), inset 0 1px 0 rgba(255,255,255,.24); }
  .pz-footer-extra-links { display: grid; gap: 10px; margin-top: 4px; }
  .pz-footer-extra-link { width: min(100%, 250px); min-height: 48px; display: inline-flex; align-items: center; justify-content: center; border-radius: 13px; padding: 12px 16px; background: rgba(255,255,255,.10); color: #fff; border: 1px solid rgba(191,219,254,.24); text-decoration: none; font-size: 15px; line-height: 1.2; font-weight: 1000; box-shadow: inset 0 1px 0 rgba(255,255,255,.10); transition: transform .18s ease, background .18s ease, border-color .18s ease; }
  .pz-footer-extra-link:hover { transform: translateY(-2px); background: rgba(219,234,254,.18); border-color: rgba(147,197,253,.42); }
  .pz-footer-platform-cta { grid-column: 1 / -1; margin-top: 10px; padding: clamp(20px, 3vw, 28px); border-radius: 18px; border: 1px solid rgba(147,197,253,.34); background: linear-gradient(135deg, rgba(11,99,246,.22), rgba(14,68,138,.30)); display: grid; grid-template-columns: 82px minmax(0, 1fr) minmax(190px, 260px) minmax(150px, auto); gap: clamp(18px, 3vw, 28px); align-items: center; box-shadow: inset 0 1px 0 rgba(255,255,255,.10), 0 18px 42px rgba(3,19,47,.20); }
  .pz-platform-icon { width: 82px; height: 82px; border-radius: 17px; display: grid; place-items: center; background: rgba(11,99,246,.24); border: 1px solid rgba(147,197,253,.36); box-shadow: inset 0 1px 0 rgba(255,255,255,.14); }
  .pz-platform-icon span { width: 40px; height: 42px; position: relative; display: block; border-radius: 0 0 7px 7px; background: #0b63f6; box-shadow: inset 13px 0 0 rgba(255,255,255,.18); }
  .pz-platform-icon span::before { content: ''; position: absolute; left: -5px; right: -5px; top: -16px; height: 17px; border-radius: 7px 7px 3px 3px; background: repeating-linear-gradient(90deg, #fff 0 10px, #0b63f6 10px 20px); }
  .pz-platform-copy h3 { margin: 0; font-size: 22px; line-height: 1.2; font-weight: 1000; color: #fff; }
  .pz-platform-copy p { margin: 9px 0 0; color: rgba(239,246,255,.88); font-size: 15px; line-height: 1.5; font-weight: 750; }
  .pz-platform-button { min-height: 58px; display: inline-flex; align-items: center; justify-content: center; border-radius: 13px; padding: 14px 20px; background: #fff; color: #0b63f6; text-decoration: none; font-size: 17px; font-weight: 1000; border: 1px solid rgba(255,255,255,.70); box-shadow: 0 16px 34px rgba(0,0,0,.18); transition: transform .18s ease, box-shadow .18s ease; }
  .pz-platform-button:hover { transform: translateY(-2px); box-shadow: 0 20px 42px rgba(0,0,0,.22); }
  .pz-platform-backed { display: grid; gap: 6px; color: #fff; }
  .pz-platform-backed small { color: rgba(255,255,255,.82); font-size: 13px; line-height: 1.2; font-weight: 750; }
  .pz-platform-backed strong { font-size: 17px; line-height: 1.2; font-weight: 1000; }
  .pz-platform-brand-lockup { display: flex; align-items: center; gap: 12px; }
  .pz-tusenda-mark { width: 46px; height: 46px; display: grid; place-items: center; flex: 0 0 auto; border-radius: 14px; overflow: hidden; background: rgba(255,255,255,.96); border: 1px solid rgba(191,219,254,.55); box-shadow: 0 12px 26px rgba(0,0,0,.18), inset 0 1px 0 rgba(255,255,255,.35); }
  .pz-tusenda-mark img { width: 100%; height: 100%; object-fit: contain; padding: 4px; display: block; }
  .pz-footer-modal-overlay { position: fixed; inset: 0; z-index: 120; display: grid; place-items: center; padding: 18px; background: rgba(15,23,42,.54); opacity: 0; pointer-events: none; transition: opacity .16s ease; }
  .pz-footer-modal-overlay.open { opacity: 1; pointer-events: auto; }
  .pz-footer-modal { position: relative; width: min(560px, 100%); max-height: min(82vh, 680px); overflow: auto; border-radius: 8px; background: #fff; color: #111827; padding: 24px; box-shadow: 0 28px 90px rgba(15,23,42,.28); }
  .pz-footer-modal-close { position: absolute; top: 12px; right: 12px; width: 38px; height: 38px; border-radius: 8px; border: 1px solid #e5e7eb; background: #fff; color: #111827; font-size: 22px; line-height: 1; font-weight: 1000; }
  .pz-footer-modal h2 { margin: 0 44px 14px 0; color: #111827; font-size: 24px; line-height: 1.1; font-weight: 1000; }
  .pz-footer-modal p { margin: 0 0 12px; color: #334155; font-size: 15px; line-height: 1.6; font-weight: 750; white-space: pre-line; }
  .pz-footer-modal ul { margin: 0 0 14px; padding-left: 20px; color: #0f172a; font-size: 15px; line-height: 1.6; font-weight: 850; }
  @media (max-width: 980px) {
    .pz-footer-main { grid-template-columns: 1fr; gap: 30px; }
    .pz-footer-platform-cta { grid-template-columns: 64px minmax(0, 1fr); }
    .pz-platform-button, .pz-platform-backed { grid-column: 1 / -1; }
    .pz-platform-backed { text-align: left; }
  }
  @media (max-width: 720px) {
    .pz-trust-grid, .pz-trust-grid.count-3, .pz-trust-grid.count-4 { grid-template-columns: repeat(2, minmax(0, 1fr)); }
    .pz-trust-grid.count-1 { grid-template-columns: 1fr; }
    .pz-trust-item { display: grid; justify-items: center; text-align: center; padding: 18px 12px; border-right: 0; border-bottom: 1px solid #e5edf7; }
    .pz-trust-icon { width: 52px; height: 52px; }
    .pz-trust-copy strong { font-size: 14px; }
    .pz-trust-copy small { font-size: 12px; }
    .pz-public-footer { border-radius: 18px; }
    .pz-footer-main { padding: 28px 18px; gap: 30px; }
    .pz-footer-store { grid-template-columns: 60px minmax(0, 1fr); }
    .pz-footer-logo-link { width: 58px; height: 58px; border-radius: 14px; }
    .pz-footer-description { font-size: 15px; }
    .pz-footer-whatsapp-btn { width: 100%; }
    .pz-footer-platform-cta { padding: 20px; grid-template-columns: 1fr; justify-items: center; text-align: center; gap: 17px; }
    .pz-platform-icon { width: 70px; height: 70px; }
    .pz-platform-button { width: 100%; }
    .pz-platform-backed { text-align: center; }
    .pz-platform-brand-lockup { justify-content: center; }
  }

  /* Footer premium: navegación útil + captación de nuevas tiendas. */
  .pz-public-footer {
    position: relative;
    isolation: isolate;
    border-radius: 28px;
    background:
      radial-gradient(circle at 88% 12%, rgba(37,99,235,.34), transparent 29%),
      radial-gradient(circle at 8% 94%, rgba(14,165,233,.13), transparent 30%),
      linear-gradient(145deg, #020b1f 0%, #061b45 46%, #03112f 100%);
    box-shadow: 0 30px 90px rgba(3,19,47,.34), inset 0 1px 0 rgba(255,255,255,.08);
  }
  .pz-public-footer::before {
    content: '';
    position: absolute;
    inset: 0;
    z-index: -1;
    pointer-events: none;
    opacity: .18;
    background-image: radial-gradient(circle, rgba(96,165,250,.7) 1px, transparent 1.5px);
    background-size: 22px 22px;
    mask-image: linear-gradient(135deg, transparent 16%, #000 52%, transparent 88%);
  }
  .pz-footer-main {
    position: relative;
    z-index: 1;
    padding: clamp(28px, 4vw, 48px);
    grid-template-columns: minmax(250px, .78fr) minmax(0, 1.42fr);
    gap: clamp(24px, 3.5vw, 44px);
  }
  .pz-footer-store {
    align-self: stretch;
    align-content: start;
    grid-template-columns: 72px minmax(0, 1fr);
    gap: 15px;
  }
  .pz-footer-logo-link {
    width: 66px;
    height: 66px;
    border-radius: 18px;
    background: rgba(255,255,255,.97);
    border-color: rgba(191,219,254,.48);
    box-shadow: 0 16px 34px rgba(0,0,0,.28), 0 0 28px rgba(59,130,246,.16);
  }
  .pz-footer-brand-copy h2 { font-size: clamp(27px, 3vw, 38px); }
  .pz-footer-description {
    margin: 16px 0 0;
    color: rgba(219,234,254,.82);
    font-size: 15px;
    line-height: 1.55;
  }
  .pz-footer-social { margin-top: 17px; gap: 9px; }
  .pz-footer-social a { width: 42px; height: 42px; }
  .pz-footer-utilities {
    min-width: 0;
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 12px;
    align-self: start;
  }
  .pz-footer-utilities.count-1 { grid-template-columns: 1fr; }
  .pz-footer-utilities.count-2 { grid-template-columns: repeat(2, minmax(0, 1fr)); }
  .pz-footer-utility {
    min-width: 0;
    min-height: 132px;
    position: relative;
    display: grid;
    grid-template-rows: auto 1fr;
    align-content: start;
    gap: 13px;
    border: 1px solid rgba(96,165,250,.25);
    border-radius: 20px;
    padding: 18px 16px;
    background: linear-gradient(145deg, rgba(8,29,71,.80), rgba(4,20,52,.66));
    color: #fff;
    text-align: left;
    text-decoration: none;
    font: inherit;
    cursor: pointer;
    box-shadow: inset 0 1px 0 rgba(255,255,255,.07), 0 16px 34px rgba(0,0,0,.14);
    transition: transform .2s ease, border-color .2s ease, background .2s ease, box-shadow .2s ease;
  }
  .pz-footer-utility:hover {
    transform: translateY(-3px);
    border-color: rgba(96,165,250,.55);
    background: linear-gradient(145deg, rgba(15,49,111,.92), rgba(5,28,70,.82));
    box-shadow: inset 0 1px 0 rgba(255,255,255,.10), 0 20px 42px rgba(0,0,0,.20);
  }
  .pz-footer-utility:focus-visible { outline: 3px solid rgba(96,165,250,.46); outline-offset: 3px; }
  .pz-footer-utility-icon {
    width: 46px;
    height: 46px;
    display: grid;
    place-items: center;
    border-radius: 15px;
    color: #60a5fa;
    background: rgba(37,99,235,.16);
    border: 1px solid rgba(96,165,250,.24);
  }
  .pz-footer-utility-icon svg { width: 25px; height: 25px; fill: currentColor; }
  .pz-footer-utility-copy { min-width: 0; display: grid; gap: 5px; padding-right: 12px; }
  .pz-footer-utility-copy strong { color: #fff; font-size: 15px; line-height: 1.18; font-weight: 1000; overflow-wrap: anywhere; }
  .pz-footer-utility-copy small { color: rgba(191,219,254,.72); font-size: 11px; line-height: 1.35; font-weight: 800; overflow-wrap: anywhere; }
  .pz-footer-utility-arrow { position: absolute; right: 14px; bottom: 16px; color: rgba(147,197,253,.68); font-size: 24px; line-height: 1; font-weight: 900; }
  .pz-footer-utility.is-whatsapp .pz-footer-utility-icon { color: #4ade80; background: rgba(34,197,94,.13); border-color: rgba(74,222,128,.28); }
  .pz-footer-secondary-links {
    grid-column: 1 / -1;
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: center;
    gap: 10px 22px;
    padding-top: 2px;
  }
  .pz-footer-secondary-links a, .pz-footer-secondary-links button {
    border: 0;
    padding: 0;
    background: transparent;
    color: rgba(191,219,254,.80);
    text-decoration: none;
    font: inherit;
    font-size: 13px;
    line-height: 1.4;
    font-weight: 850;
    cursor: pointer;
  }
  .pz-footer-secondary-links a:hover, .pz-footer-secondary-links button:hover { color: #fff; }
  .pz-footer-platform-cta {
    grid-area: auto;
    grid-column: 1 / -1;
    margin-top: 2px;
    padding: clamp(24px, 4vw, 44px);
    border-radius: 25px;
    border: 1px solid rgba(59,130,246,.72);
    background:
      radial-gradient(circle at 76% 38%, rgba(37,99,235,.27), transparent 28%),
      linear-gradient(135deg, rgba(6,34,86,.97), rgba(2,16,45,.98));
    display: grid;
    grid-template-columns: minmax(0, 1.05fr) minmax(260px, .95fr);
    grid-template-areas:
      'copy visual'
      'button visual'
      'backed backed';
    gap: 22px clamp(24px, 4vw, 54px);
    overflow: hidden;
    box-shadow: inset 0 1px 0 rgba(255,255,255,.08), 0 22px 52px rgba(0,0,0,.25), 0 0 0 1px rgba(37,99,235,.08);
  }
  .pz-platform-copy { grid-area: copy; align-self: end; }
  .pz-platform-eyebrow {
    display: block;
    margin-bottom: 12px;
    color: #38bdf8;
    font-size: 12px;
    line-height: 1.2;
    font-weight: 1000;
    letter-spacing: .14em;
    text-transform: uppercase;
  }
  .pz-platform-copy h3 { max-width: 560px; font-size: clamp(28px, 4vw, 46px); line-height: 1.05; letter-spacing: -.025em; }
  .pz-platform-copy p { max-width: 540px; margin-top: 15px; color: rgba(219,234,254,.82); font-size: clamp(14px, 1.5vw, 17px); line-height: 1.58; }
  .pz-platform-button {
    grid-area: button;
    width: min(100%, 430px);
    min-height: 62px;
    border-radius: 16px;
    background: #fff;
    color: #073b9b;
    font-size: 17px;
    box-shadow: 0 18px 40px rgba(0,0,0,.25), inset 0 -2px 0 rgba(15,23,42,.06);
  }
  .pz-platform-visual {
    grid-area: visual;
    min-height: 260px;
    position: relative;
    display: grid;
    place-items: center;
    align-self: stretch;
  }
  .pz-platform-visual::before {
    content: '';
    position: absolute;
    width: 72%;
    aspect-ratio: 1;
    border-radius: 50%;
    background: radial-gradient(circle, rgba(37,99,235,.34), rgba(37,99,235,.04) 58%, transparent 70%);
    filter: blur(2px);
  }
  .pz-platform-orbit { position: absolute; width: 74%; aspect-ratio: 1.7; border: 1px solid rgba(56,189,248,.24); border-radius: 50%; transform: rotate(-18deg); }
  .pz-platform-orbit-two { width: 58%; transform: rotate(24deg); border-color: rgba(96,165,250,.18); }
  .pz-platform-window {
    width: min(310px, 88%);
    height: 210px;
    position: relative;
    z-index: 1;
    border-radius: 24px;
    background: linear-gradient(145deg, rgba(21,64,145,.98), rgba(5,25,66,.98));
    border: 1px solid rgba(96,165,250,.48);
    box-shadow: 0 28px 60px rgba(0,0,0,.32), inset 0 1px 0 rgba(255,255,255,.10), 0 0 38px rgba(37,99,235,.18);
    transform: perspective(700px) rotateY(-7deg) rotateX(3deg);
  }
  .pz-platform-window-top { height: 34px; display: flex; align-items: center; gap: 6px; padding: 0 14px; border-bottom: 1px solid rgba(147,197,253,.20); }
  .pz-platform-window-top i { width: 7px; height: 7px; border-radius: 50%; background: rgba(147,197,253,.52); }
  .pz-platform-shop { position: absolute; left: 22px; top: 64px; width: 116px; height: 112px; border-radius: 12px; background: linear-gradient(180deg, #0b63f6, #0647b8); box-shadow: 0 15px 30px rgba(0,0,0,.24); }
  .pz-platform-shop::before { content: ''; position: absolute; left: -5px; right: -5px; top: -20px; height: 25px; border-radius: 9px 9px 5px 5px; background: repeating-linear-gradient(90deg, #fff 0 20px, #1d7cff 20px 40px); box-shadow: 0 8px 16px rgba(0,0,0,.16); }
  .pz-platform-shop i { position: absolute; left: 45px; bottom: 0; width: 30px; height: 55px; border-radius: 7px 7px 0 0; background: #031c4e; }
  .pz-platform-chart { position: absolute; right: 20px; top: 62px; width: 126px; height: 62px; border-radius: 12px; background: linear-gradient(135deg, rgba(14,165,233,.20), rgba(37,99,235,.13)); border: 1px solid rgba(56,189,248,.22); }
  .pz-platform-chart::before { content: ''; position: absolute; left: 16px; right: 15px; top: 31px; height: 3px; background: linear-gradient(100deg, transparent 0 7%, #38bdf8 8% 32%, #60a5fa 33% 56%, #22d3ee 57% 100%); clip-path: polygon(0 78%, 22% 35%, 45% 60%, 67% 10%, 100% 40%, 100% 100%, 0 100%); }
  .pz-platform-product { position: absolute; right: 31px; bottom: 22px; width: 100px; height: 55px; border-radius: 12px; background: rgba(219,234,254,.10); border: 1px solid rgba(147,197,253,.20); }
  .pz-platform-product::before { content: ''; position: absolute; left: 11px; top: 10px; width: 34px; height: 34px; border-radius: 9px; background: linear-gradient(145deg, #60a5fa, #1d4ed8); }
  .pz-platform-product::after { content: ''; position: absolute; left: 54px; right: 10px; top: 17px; height: 5px; border-radius: 5px; background: rgba(219,234,254,.72); box-shadow: 0 12px 0 rgba(147,197,253,.36); }
  .pz-platform-backed {
    grid-area: backed;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 12px;
    padding-top: 20px;
    border-top: 1px solid rgba(96,165,250,.16);
  }
  .pz-platform-backed > span:last-child { display: grid; gap: 2px; text-align: left; }
  .pz-platform-backed small { color: rgba(191,219,254,.70); font-size: 11px; text-transform: uppercase; letter-spacing: .08em; }
  .pz-platform-backed strong { font-size: 16px; }
  .pz-tusenda-mark { width: 44px; height: 44px; border-radius: 13px; }

  @media (max-width: 980px) {
    .pz-footer-main { grid-template-columns: 1fr; }
    .pz-footer-utilities { grid-template-columns: repeat(3, minmax(0, 1fr)); }
    .pz-footer-platform-cta {
      grid-template-columns: minmax(0, 1fr) minmax(220px, .8fr);
      grid-template-areas: 'copy visual' 'button visual' 'backed backed';
    }
  }
  @media (max-width: 720px) {
    .pz-public-footer { border-radius: 24px; }
    .pz-footer-main { padding: 24px 16px; gap: 24px; }
    .pz-footer-store { grid-template-columns: 58px minmax(0, 1fr); }
    .pz-footer-logo-link { width: 54px; height: 54px; border-radius: 15px; }
    .pz-footer-brand-copy h2 { font-size: 27px; }
    .pz-footer-description { margin-top: 12px; font-size: 13px; }
    .pz-footer-utilities { gap: 8px; }
    .pz-footer-utility { min-height: 124px; padding: 13px 10px; border-radius: 17px; justify-items: center; text-align: center; }
    .pz-footer-utility-icon { width: 42px; height: 42px; border-radius: 14px; }
    .pz-footer-utility-copy { padding-right: 0; }
    .pz-footer-utility-copy strong { font-size: 12px; }
    .pz-footer-utility-copy small { font-size: 9px; }
    .pz-footer-utility-arrow { display: none; }
    .pz-footer-secondary-links { gap: 9px 16px; }
    .pz-footer-platform-cta {
      padding: 24px 18px;
      grid-template-columns: 1fr;
      grid-template-areas: 'copy' 'visual' 'button' 'backed';
      justify-items: stretch;
      text-align: left;
      gap: 20px;
    }
    .pz-platform-copy h3 { font-size: clamp(29px, 9vw, 39px); }
    .pz-platform-copy p { font-size: 14px; }
    .pz-platform-visual { min-height: 220px; }
    .pz-platform-window { width: min(300px, 94%); height: 195px; }
    .pz-platform-button { width: 100%; }
    .pz-platform-backed { justify-content: flex-start; text-align: left; }
  }
  @media (max-width: 380px) {
    .pz-footer-utility { min-height: 116px; padding-inline: 7px; }
    .pz-footer-utility-copy small { display: none; }
    .pz-platform-window { transform: scale(.94); }
  }

  /* Composición final basada en el concepto premium aprobado. */
  .pz-footer-utilities {
    gap: 0;
    overflow: hidden;
    border: 1px solid rgba(59,130,246,.38);
    border-radius: 26px;
    background: linear-gradient(145deg, rgba(5,26,68,.88), rgba(2,14,39,.76));
    box-shadow: inset 0 1px 0 rgba(255,255,255,.08), 0 18px 42px rgba(0,0,0,.18), 0 0 34px rgba(37,99,235,.08);
  }
  .pz-footer-utility {
    min-height: 174px;
    place-items: center;
    align-content: center;
    grid-template-rows: auto auto;
    gap: 17px;
    padding: 24px 12px;
    border: 0;
    border-radius: 0;
    background: transparent;
    text-align: center;
    box-shadow: none;
  }
  .pz-footer-utility:not(:first-child) { border-left: 1px solid rgba(96,165,250,.24); }
  .pz-footer-utility:hover {
    transform: none;
    border-color: rgba(96,165,250,.24);
    background: linear-gradient(180deg, rgba(37,99,235,.16), rgba(37,99,235,.04));
    box-shadow: inset 0 0 34px rgba(37,99,235,.10);
  }
  .pz-footer-utility-icon {
    width: 64px;
    height: 64px;
    border-radius: 50%;
    color: #60a5fa;
    background: linear-gradient(145deg, rgba(13,53,128,.94), rgba(6,30,82,.92));
    border-color: rgba(96,165,250,.20);
    box-shadow: inset 0 1px 0 rgba(255,255,255,.10), inset 0 0 20px rgba(37,99,235,.10), 0 12px 28px rgba(0,0,0,.18);
  }
  .pz-footer-utility-icon svg {
    width: 32px;
    height: 32px;
    overflow: visible;
    filter: drop-shadow(0 3px 4px rgba(0,0,0,.30)) drop-shadow(0 0 5px color-mix(in srgb, currentColor 38%, transparent));
  }
  .pz-footer-utility-icon img {
    width: 100%;
    height: 100%;
    display: block;
    object-fit: contain;
    filter: drop-shadow(0 7px 9px rgba(0,0,0,.26));
  }
  .pz-footer-utility-copy { padding: 0; display: block; }
  .pz-footer-utility-copy strong { font-size: clamp(14px, 1.7vw, 18px); line-height: 1.18; }
  .pz-footer-utility.is-whatsapp .pz-footer-utility-icon {
    color: #52f39a;
    background: linear-gradient(145deg, rgba(6,78,74,.82), rgba(4,43,62,.94));
    border-color: rgba(82,243,154,.28);
  }
  .pz-footer-utility-arrow { display: none; }

  .pz-footer-platform-cta {
    grid-template-columns: minmax(0, 1.02fr) minmax(260px, .98fr);
    grid-template-areas:
      'copy visual'
      'button button'
      'learn learn';
    gap: 22px clamp(18px, 3vw, 42px);
    border: 2px solid rgba(52,125,255,.84);
    background:
      radial-gradient(circle at 78% 38%, rgba(37,99,235,.30), transparent 30%),
      linear-gradient(138deg, rgba(4,31,80,.99), rgba(1,13,37,.99));
    box-shadow: inset 0 1px 0 rgba(255,255,255,.10), 0 24px 56px rgba(0,0,0,.28), 0 0 28px rgba(37,99,235,.20);
  }
  .pz-platform-visual {
    min-height: 330px;
    overflow: visible;
  }
  .pz-platform-visual::before {
    width: 90%;
    background: radial-gradient(circle, rgba(37,99,235,.30), rgba(37,99,235,.04) 58%, transparent 72%);
  }
  .pz-platform-visual img {
    position: relative;
    z-index: 1;
    width: min(112%, 430px);
    height: auto;
    display: block;
    object-fit: contain;
    filter: drop-shadow(0 28px 34px rgba(0,0,0,.30));
  }
  .pz-platform-button {
    width: 100%;
    max-width: none;
    min-height: 72px;
    border-radius: 18px;
    font-size: clamp(17px, 2vw, 21px);
  }
  .pz-platform-learn {
    grid-area: learn;
    justify-self: center;
    display: inline-flex;
    align-items: center;
    gap: 16px;
    border: 0;
    padding: 5px 12px;
    background: transparent;
    color: #38bdf8;
    font: inherit;
    font-size: 17px;
    line-height: 1.2;
    font-weight: 850;
    cursor: pointer;
  }
  .pz-platform-learn span { font-size: 30px; line-height: .7; transition: transform .18s ease; }
  .pz-platform-learn:hover span { transform: translateX(4px); }
  .pz-platform-learn:focus-visible { outline: 3px solid rgba(56,189,248,.48); outline-offset: 5px; border-radius: 8px; }
  .pz-footer-platform-backed {
    grid-column: 1 / -1;
    min-height: 132px;
    position: relative;
    overflow: hidden;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 18px;
    padding: 24px;
    border: 1px solid rgba(59,130,246,.30);
    border-radius: 24px;
    color: #fff;
    background: linear-gradient(145deg, rgba(5,25,64,.88), rgba(2,14,39,.80));
    box-shadow: inset 0 1px 0 rgba(255,255,255,.06), 0 18px 38px rgba(0,0,0,.16);
  }
  .pz-footer-platform-backed::before,
  .pz-footer-platform-backed::after {
    content: '';
    position: absolute;
    bottom: -28px;
    width: 240px;
    height: 78px;
    opacity: .24;
    background: repeating-radial-gradient(ellipse at center, transparent 0 7px, #2563eb 8px 9px, transparent 10px 15px);
  }
  .pz-footer-platform-backed::before { left: -72px; transform: rotate(8deg); }
  .pz-footer-platform-backed::after { right: -72px; transform: rotate(-8deg); }
  .pz-footer-platform-backed > span:last-child { position: relative; z-index: 1; display: grid; gap: 3px; text-align: left; }
  .pz-footer-platform-backed small { color: rgba(219,234,254,.78); font-size: 15px; line-height: 1.2; }
  .pz-footer-platform-backed strong { color: #fff; font-size: 24px; line-height: 1.15; font-weight: 1000; }
  .pz-footer-platform-backed .pz-tusenda-mark { position: relative; z-index: 1; width: 70px; height: 70px; border-radius: 18px; }
  .pz-footer-modal-action {
    min-height: 50px;
    margin-top: 8px;
    padding: 13px 18px;
    border-radius: 12px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    background: #0b63f6;
    color: #fff;
    text-decoration: none;
    font-size: 15px;
    font-weight: 1000;
  }

  @media (max-width: 980px) {
    .pz-footer-platform-cta {
      grid-template-columns: minmax(0, 1fr) minmax(230px, .92fr);
      grid-template-areas: 'copy visual' 'button button' 'learn learn';
    }
  }
  @media (max-width: 720px) {
    .pz-footer-main { padding: 28px 20px; gap: 25px; }
    .pz-footer-store { grid-template-columns: 78px minmax(0, 1fr); gap: 17px; }
    .pz-footer-logo-link { width: 76px; height: 76px; border-radius: 20px; }
    .pz-footer-brand-copy h2 { font-size: 32px; }
    .pz-footer-description { margin-top: 16px; font-size: 16px; line-height: 1.5; }
    .pz-footer-utilities { gap: 0; border-radius: 22px; }
    .pz-footer-utility {
      min-height: 146px;
      gap: 13px;
      padding: 18px 7px;
      border-radius: 0;
    }
    .pz-footer-utility:not(:first-child) { border-left: 1px solid rgba(96,165,250,.24); }
    .pz-footer-utility-icon { width: 54px; height: 54px; border-radius: 50%; }
    .pz-footer-utility-icon svg { width: 28px; height: 28px; }
    .pz-footer-utility-icon img { width: 100%; height: 100%; }
    .pz-footer-utility-copy strong { font-size: clamp(11px, 3.25vw, 14px); }
    .pz-footer-platform-cta {
      padding: 27px 20px;
      grid-template-columns: minmax(0, .98fr) minmax(0, 1.02fr);
      grid-template-areas: 'copy visual' 'button button' 'learn learn';
      justify-items: stretch;
      text-align: left;
      gap: 20px 10px;
      border-radius: 24px;
    }
    .pz-platform-copy { align-self: center; }
    .pz-platform-copy h3 { font-size: clamp(28px, 8vw, 35px); line-height: 1.06; }
    .pz-platform-copy p { margin-top: 15px; font-size: 13px; line-height: 1.55; }
    .pz-platform-eyebrow { margin-bottom: 11px; font-size: 10px; }
    .pz-platform-visual { min-height: 260px; align-self: center; }
    .pz-platform-visual img { width: 118%; max-width: 245px; transform: translateX(2%); }
    .pz-platform-button { min-height: 62px; font-size: 17px; }
    .pz-platform-learn { font-size: 15px; }
    .pz-footer-platform-backed { min-height: 118px; border-radius: 22px; }
    .pz-footer-platform-backed .pz-tusenda-mark { width: 60px; height: 60px; border-radius: 16px; }
    .pz-footer-platform-backed small { font-size: 13px; }
    .pz-footer-platform-backed strong { font-size: 21px; }
  }
  @media (max-width: 380px) {
    .pz-footer-main { padding-inline: 15px; }
    .pz-footer-utility { min-height: 132px; padding-inline: 5px; }
    .pz-footer-utility-icon { width: 48px; height: 48px; }
    .pz-footer-platform-cta { padding-inline: 16px; }
    .pz-platform-copy h3 { font-size: 27px; }
    .pz-platform-visual { min-height: 235px; }
  }
</style>
