---
interface Props {
  id: string;
  label: string;
  class?: string;
}

const { id, label, class: className = '' } = Astro.props;
---

<div
  id={id}
  class:list={['master-action-menu', className]}
  role="menu"
  aria-label={label}
  data-master-action-menu
  hidden
>
  <slot />
</div>
